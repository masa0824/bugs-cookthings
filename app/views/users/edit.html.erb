<h2>ユーザ編集画面</h2>
<div id="users">
  <div class="contents-wrapp">
    <%= form_for(@user, url: users_path) do |f| %>
      <div class="input-form">
        メールアドレス：<%= f.email_field :email, :placeholder => 'メールアドレス', :class => 'input-field', :maxlength => 255, :disabled => 'disabled' %>
        氏名(姓)：<%= f.text_field :last_name, :placeholder => '氏名(姓)', :class => 'input-field', :maxlength => 255 %>
        氏名(名)：<%= f.text_field :first_name, :placeholder => '氏名(名)', :class => 'input-field', :maxlength => 255 %>
        <%= f.submit "変更", :class => 'btn create-btn' %>
        <%= link_to "パスワードを変更する", edit_pw_path, :class => 'link' %>
        <%= link_to "アカウントを削除する", account_delete_path, :class => 'link' %>
        使用容量：<%= @capacity %> (利用上限：<%= @user_limit_info['USER_LIMIT_CAPACITY'] %>MB)<br />
        登録数：<%= @regist_recipe %>レシピ (利用上限：<%= @user_limit_info['USER_LIMIT_REGIST_RECIPE'] %>レシピ)
      </div>
    <% end %>
  </div>
</div>